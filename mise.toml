[tools]
erlang = "29"
elixir = "1.20-otp-29"
git-cliff = "2"

[hooks]
# Runs after every `mise install`, including one that installs nothing.
postinstall = "mix deps.get"

[task_config]
# File tasks live in scripts/ (`mise run release`, `mise run test:release`).
includes = ["scripts"]
